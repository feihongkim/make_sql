# 빌드 바이너리
abledb
abledb_Hope

# 설정/인증 (민감정보)
config.yaml
*.pem
*.key
docker-config/ssh/
docker-config/telegram/.env
mcp/google/token.json
mcp/google/credentials.json

# Claude Code
.claude/
.telegram/
CLAUDE.md

# Docker
Dockerfile
docker-compose.yml
docker-config/

# MCP
mcp/notion/

# 로그
nginx_log/
test_logs/
python/.venv/

# 백업 파일 — 변경 전 내용은 git 이력에 남으므로 저장소에 둘 이유가 없다
*.bak
*.bak_*
*.orig
